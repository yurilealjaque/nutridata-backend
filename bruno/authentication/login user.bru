meta {
  name: login user
  type: http
  seq: 2
}

post {
  url: {{apiUrl}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "john.doe@example.com",
    "password": "root123" 
  }
}

settings {
  encodeUrl: true
}
